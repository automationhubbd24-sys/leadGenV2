version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
  caddy:
    image: caddy:2.7.6
    ports:
      - "80:80"
    volumes:
      - ./dist:/srv
      - ./Caddyfile:/etc/caddy/Caddyfile
    depends_on:
      - app
